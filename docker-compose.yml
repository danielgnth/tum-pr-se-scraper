services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: tum
      POSTGRES_PASSWORD: tum
      POSTGRES_DB: courses
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
